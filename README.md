Primeiro rode comando npm install para installar todas as dependências.<br/>
Para iniciar o banco de dados rode o comando docker-compose up -d. <br/>
Então rode npm run dev para startar o servidor node.js.
